# QM CLI

The CLI design has shipped. The normative deployment-directory contract is
[`deploy-directory.md`](./deploy-directory.md), and the operator walkthrough is
[`getting-started.md`](./getting-started.md). Run `qm help` for the current
command surface.

The implementation and package documentation live in [`../cli/`](../cli/).
